pre-commit
rich
openai
tiktoken
# torch and transformers are only needed for the StarCoder (HuggingFace) backend.
# The OpenAI-compatible (openai/...) and ollama paths do not require them.
# Install via: pip install torch transformers   (or torch --index-url ... for CPU-only)
python-dotenv
click
PyYAML
qiskit==0.43.1
toml
pyparsing
fastcov
matplotlib
coverage==7.2.7
pandas==2.0.3
ollama
requests
