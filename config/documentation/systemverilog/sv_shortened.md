Please create a very short, self-contained SystemVerilog (IEEE 1800-2012) program that both iverilog (`-g2012`) and verilator (`--sv`) can elaborate without errors. The top-level module must be named `top` and must be the only `module` in the file. Use basic synthesizable features: `logic` signals, `parameter`/`localparam`, packed structs, enums (`typedef enum logic [N:0] { ... } t;`), `always_ff @(posedge clk or negedge rst_n)`, `always_comb`, simple `case`/`if`, arithmetic and bitwise ops, `assign`, and `generate`/`for`. Do not use `interface`, `clocking`, `assert property`, `cover property`, concurrent assertions, `import`, `` `include ``, classes, dynamic arrays/queues, constraints, `fork`/`join`, or DPI. Keep the module under ~40 lines. Output only the module code, nothing else.